interface:
  display_name: "HelmCode App Testing"
  short_description: "Launch and retain isolated HelmCode test environments"
  default_prompt: "Use $test-helmcode-app to launch an isolated HelmCode environment and iteratively test it in the browser while preserving state."
